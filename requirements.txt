# Core ML/DL frameworks
torch>=2.0.0
torchvision>=0.15.0

# Computer Vision
opencv-python>=4.8.0
albumentations>=1.3.1

# Data handling
numpy>=1.24.0
pillow>=10.0.0

# XML parsing (included in Python, but explicit for clarity)
lxml>=4.9.0

# Progress bars & logging
tqdm>=4.65.0

# For training history & config
# (json is built-in, no need to install)

# Optional: For visualization/demo (install when needed)
matplotlib>=3.7.0